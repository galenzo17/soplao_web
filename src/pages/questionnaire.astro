<div class="questionnaire">
    <h2 class="text-2xl font-bold text-center mb-6">Formulario de Cotización – Soplao</h2>

    <form id="questionnaireForm" class="space-y-6">
      <!-- Sección: Datos del Servicio -->
      <div class="bg-gray-50 p-6 rounded-lg">
        <h3 class="text-xl font-semibold mb-4 flex items-center">
          <span class="mr-2">🏠</span> Datos del servicio
        </h3>

        <div class="space-y-4">
          <!-- Pregunta 1: m² -->
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-2">
              ¿Cuántos m² tiene tu casa, departamento u oficina? *
            </label>
            <input type="number" name="squareMeters" class="w-full px-4 py-2 border border-gray-300 rounded-md focus:ring-2 focus:ring-brand focus:border-brand" required>
          </div>

          <!-- Pregunta 2: Tipo de limpieza -->
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-2">
              ¿Requieres un servicio de limpieza profunda o de mantenimiento (básica)? *
            </label>
            <select name="cleaningType" class="w-full px-4 py-2 border border-gray-300 rounded-md focus:ring-2 focus:ring-brand focus:border-brand" required>
              <option value="">Selecciona una opción</option>
              <option value="mantenimiento">Mantenimiento (básica)</option>
              <option value="profunda">Limpieza profunda</option>
            </select>
          </div>

          <!-- Pregunta 3: Ubicación -->
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-2">
              ¿Dónde queda ubicado tu domicilio? (comuna o sector) *
            </label>
            <input type="text" name="location" placeholder="Ej: Las Condes, Providencia, etc." class="w-full px-4 py-2 border border-gray-300 rounded-md focus:ring-2 focus:ring-brand focus:border-brand" required>
          </div>

          <!-- Pregunta 4: Pisos (condicional si es casa) -->
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-2">
              Si es casa, ¿cuántos pisos tiene?
            </label>
            <select name="floors" class="w-full px-4 py-2 border border-gray-300 rounded-md focus:ring-2 focus:ring-brand focus:border-brand">
              <option value="">No aplica / No es casa</option>
              <option value="1">1 piso</option>
              <option value="2">2 pisos</option>
              <option value="3">3 pisos</option>
              <option value="4+">4 o más pisos</option>
            </select>
          </div>

          <!-- Pregunta 5: Amoblado o vacío -->
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-2">
              ¿Tu espacio está amoblado o vacío? *
            </label>
            <select name="furnished" class="w-full px-4 py-2 border border-gray-300 rounded-md focus:ring-2 focus:ring-brand focus:border-brand" required>
              <option value="">Selecciona una opción</option>
              <option value="amoblado">Amoblado</option>
              <option value="vacio">Vacío</option>
              <option value="parcialmente">Parcialmente amoblado</option>
            </select>
          </div>

          <!-- Pregunta 6: Última limpieza -->
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-2">
              ¿Hace cuánto fue la última limpieza completa del lugar? *
            </label>
            <select name="lastCleaning" class="w-full px-4 py-2 border border-gray-300 rounded-md focus:ring-2 focus:ring-brand focus:border-brand" required>
              <option value="">Selecciona una opción</option>
              <option value="menos-1-mes">Menos de 1 mes</option>
              <option value="1-3-meses">Entre 1 y 3 meses</option>
              <option value="3-6-meses">Entre 3 y 6 meses</option>
              <option value="mas-6-meses">Más de 6 meses</option>
              <option value="nunca">Nunca / No recuerdo</option>
            </select>
          </div>

          <!-- Pregunta 7: Tipo de piso -->
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-2">
              Indícanos cuál es el tipo de piso y si tiene alfombras decorativas o de muro a muro. *
            </label>
            <textarea name="floorType" rows="3" placeholder="Ej: Porcelanato en toda la casa, alfombra de muro a muro en dormitorios" class="w-full px-4 py-2 border border-gray-300 rounded-md focus:ring-2 focus:ring-brand focus:border-brand" required></textarea>
          </div>

          <!-- Pregunta 8: Baños y dormitorios -->
          <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
            <div>
              <label class="block text-sm font-medium text-gray-700 mb-2">
                ¿Cuántos baños tiene el lugar? *
              </label>
              <select name="bathrooms" class="w-full px-4 py-2 border border-gray-300 rounded-md focus:ring-2 focus:ring-brand focus:border-brand" required>
                <option value="">Selecciona</option>
                <option value="1">1</option>
                <option value="2">2</option>
                <option value="3">3</option>
                <option value="4">4</option>
                <option value="5+">5 o más</option>
              </select>
            </div>
            <div>
              <label class="block text-sm font-medium text-gray-700 mb-2">
                ¿Cuántos dormitorios tiene el lugar? *
              </label>
              <select name="bedrooms" class="w-full px-4 py-2 border border-gray-300 rounded-md focus:ring-2 focus:ring-brand focus:border-brand" required>
                <option value="">Selecciona</option>
                <option value="1">1</option>
                <option value="2">2</option>
                <option value="3">3</option>
                <option value="4">4</option>
                <option value="5+">5 o más</option>
              </select>
            </div>
          </div>

          <!-- Pregunta 9: Solo interior -->
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-2">
              ¿El servicio que solicitas es solo interior? (Si tu balcón no supera los 2 m² se considera interior.) *
            </label>
            <div class="space-y-2">
              <label class="flex items-center space-x-3">
                <input type="radio" name="interiorOnly" value="si" class="form-radio text-brand" required>
                <span>Sí, solo interior</span>
              </label>
              <label class="flex items-center space-x-3">
                <input type="radio" name="interiorOnly" value="no" class="form-radio text-brand">
                <span>No, incluye áreas exteriores</span>
              </label>
            </div>
          </div>

          <!-- Pregunta 10: Limpieza de vidrios -->
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-2">
              ¿Requieres servicio de limpieza de vidrios? (Cualquier servicio incluye solo la limpieza de espejos de baño.) *
            </label>
            <div class="space-y-2">
              <label class="flex items-center space-x-3">
                <input type="radio" name="windowCleaning" value="si" class="form-radio text-brand" required>
                <span>Sí</span>
              </label>
              <label class="flex items-center space-x-3">
                <input type="radio" name="windowCleaning" value="no" class="form-radio text-brand">
                <span>No</span>
              </label>
            </div>
          </div>

          <!-- Pregunta 11: Lavado de alfombra -->
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-2">
              ¿Deseas incluir servicio de lavado de alfombra? Si es así, ¿de cuántos m² es aproximadamente?
            </label>
            <input type="text" name="carpetCleaning" placeholder="Ej: Sí, aproximadamente 15 m² / No" class="w-full px-4 py-2 border border-gray-300 rounded-md focus:ring-2 focus:ring-brand focus:border-brand">
          </div>

          <!-- Pregunta 12: Estacionamiento -->
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-2">
              ¿Cuentas con estacionamiento disponible mientras dure el servicio? *
            </label>
            <div class="space-y-2">
              <label class="flex items-center space-x-3">
                <input type="radio" name="parking" value="si" class="form-radio text-brand" required>
                <span>Sí</span>
              </label>
              <label class="flex items-center space-x-3">
                <input type="radio" name="parking" value="no" class="form-radio text-brand">
                <span>No</span>
              </label>
            </div>
          </div>

          <!-- Pregunta 13: Mascotas -->
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-2">
              ¿Tienes mascotas en casa? *
            </label>
            <div class="space-y-2">
              <label class="flex items-center space-x-3">
                <input type="radio" name="pets" value="si" class="form-radio text-brand" required>
                <span>Sí</span>
              </label>
              <label class="flex items-center space-x-3">
                <input type="radio" name="pets" value="no" class="form-radio text-brand">
                <span>No</span>
              </label>
            </div>
          </div>

          <!-- Pregunta 14: Fotos -->
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-2">
              ¿Nos podrías enviar algunas fotos de tu casa u oficina para evaluar mejor el estado y preparar el presupuesto?
            </label>
            <input type="file" name="photos" multiple accept="image/*" class="w-full px-4 py-2 border border-gray-300 rounded-md focus:ring-2 focus:ring-brand focus:border-brand">
            <p class="text-xs text-gray-500 mt-1">Puedes seleccionar múltiples fotos</p>
          </div>

          <!-- Pregunta 15: Día y horario -->
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-2">
              ¿Qué día y horario te gustaría agendar el servicio? *
            </label>
            <input type="text" name="preferredDateTime" placeholder="Ej: Lunes 15 de marzo, 9:00 AM" class="w-full px-4 py-2 border border-gray-300 rounded-md focus:ring-2 focus:ring-brand focus:border-brand" required>
          </div>

          <!-- Pregunta 16: Boleta o factura -->
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-2">
              ¿Deseas recibir boleta o factura? *
            </label>
            <select name="invoiceType" class="w-full px-4 py-2 border border-gray-300 rounded-md focus:ring-2 focus:ring-brand focus:border-brand" required>
              <option value="">Selecciona una opción</option>
              <option value="boleta">Boleta</option>
              <option value="factura">Factura</option>
              <option value="no-requiere">No requiero</option>
            </select>
          </div>
        </div>
      </div>

      <!-- Sección: Datos de Contacto -->
      <div class="bg-gray-50 p-6 rounded-lg">
        <h3 class="text-xl font-semibold mb-4 flex items-center">
          <span class="mr-2">📞</span> Datos de contacto
        </h3>

        <div class="space-y-4">
          <!-- Nombre completo -->
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-2">
              Nombre completo: *
            </label>
            <input type="text" name="fullName" class="w-full px-4 py-2 border border-gray-300 rounded-md focus:ring-2 focus:ring-brand focus:border-brand" required>
          </div>

          <!-- Teléfono -->
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-2">
              Número de teléfono o WhatsApp: *
            </label>
            <input type="tel" name="phone" placeholder="+56 9 1234 5678" class="w-full px-4 py-2 border border-gray-300 rounded-md focus:ring-2 focus:ring-brand focus:border-brand" required>
          </div>

          <!-- Email -->
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-2">
              Correo electrónico: *
            </label>
            <input type="email" name="email" placeholder="tu@email.com" class="w-full px-4 py-2 border border-gray-300 rounded-md focus:ring-2 focus:ring-brand focus:border-brand" required>
          </div>

          <!-- Cómo nos conociste -->
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-2">
              ¿Cómo nos conociste? *
            </label>
            <select name="referralSource" class="w-full px-4 py-2 border border-gray-300 rounded-md focus:ring-2 focus:ring-brand focus:border-brand" required>
              <option value="">Selecciona una opción</option>
              <option value="redes-sociales">Redes sociales</option>
              <option value="recomendacion">Recomendación</option>
              <option value="afiche">Afiche</option>
              <option value="google">Búsqueda en Google</option>
              <option value="otro">Otro</option>
            </select>
          </div>
        </div>
      </div>

      <!-- Botón de envío -->
      <div class="text-center">
        <button type="submit" class="bg-brand text-white font-semibold py-3 px-8 rounded-md hover:bg-brand/90 transition-colors duration-200 text-lg">
          Enviar Cotización
        </button>
      </div>
    </form>
    
    <!-- Resultado -->
    <div id="result" class="hidden">
      <div class="bg-green-50 border border-green-200 rounded-md p-6 text-center">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-12 w-12 text-green-500 mx-auto mb-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
        </svg>
        <h3 class="text-xl font-semibold text-green-800 mb-3">¡Gracias por tu interés en Soplao!</h3>
        <p class="text-green-700 mb-2">
          Te responderemos a la brevedad con una cotización ajustada a tus necesidades.
        </p>
        <button id="resetForm" class="mt-4 bg-white text-brand border border-brand font-semibold py-2 px-4 rounded-md hover:bg-gray-50 transition-colors duration-200">
          Completar de nuevo
        </button>
      </div>
    </div>
  </div>

  <script>
    document.addEventListener('DOMContentLoaded', () => {
      const form = document.getElementById('questionnaireForm');
      const result = document.getElementById('result');
      const resetButton = document.getElementById('resetForm');

      // Evento para el envío del formulario
      form?.addEventListener('submit', (e) => {
        e.preventDefault();

        // Scroll al inicio para mostrar el mensaje de confirmación
        window.scrollTo({ top: 0, behavior: 'smooth' });

        form.classList.add('hidden');
        result?.classList.remove('hidden');

        // Aquí se podría añadir la lógica para enviar los datos al servidor
        // const formData = new FormData(form);
        // fetch('/api/questionnaire', {
        //   method: 'POST',
        //   body: formData
        // });
      });

      // Evento para reiniciar el formulario
      resetButton?.addEventListener('click', () => {
        form?.reset();
        form?.classList.remove('hidden');
        result?.classList.add('hidden');
        window.scrollTo({ top: 0, behavior: 'smooth' });
      });
    });
  </script>
  
  <style>
    
  </style>